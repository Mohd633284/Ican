rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true; // Allow all for testing
    }
  }
}

// ⚠️ WARNING: This is for testing only. Revert to authenticated rules for production!
